# Install Billy 0.6.3

Replace the existing `custom_components/bill_tracker` directory with the one from this archive, restart Home Assistant, then hard-refresh the frontend.

The frontend remains the complete 0.5.2-based UI restored in 0.6.2. Version 0.6.3 only adds parser/import correctness fixes required by providers such as TIM.
